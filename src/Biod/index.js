import React from 'react';
import LocalS from "./LocalS";

class LocalS extends React.PureComponent {
    render () { 
        return (
            <main className="content"> 
              <h1 className="title">Setting page</h1>
            </main>

        );
    }
}
